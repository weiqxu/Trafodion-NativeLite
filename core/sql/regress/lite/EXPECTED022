>>SELECT CHAR_LENGTH(SPACE(3)) AS positive_count,
+>       CHAR_LENGTH(SPACE(0)) AS zero_count,
+>       CASE WHEN SPACE(CAST(NULL AS INT)) IS NULL
+>            THEN 'NULL' ELSE 'VALUE' END AS null_state,
+>       CHAR_LENGTH('L' || SPACE(3) || 'R') AS wrapped_count
+>  FROM (VALUES (1)) AS v(n);
POSITIVE_COUNT  ZERO_COUNT  NULL_STATE  WRAPPED_COUNT
--------------  ----------  ----------  -------------
             3           0  NULL                    5
--- 1 row(s) selected.
>>CREATE TABLE LITEREG022_SPACE_COUNTS
+>  (id INT PRIMARY KEY,
+>   space_count INT);
--- SQL operation complete.
>>INSERT INTO LITEREG022_SPACE_COUNTS VALUES
+>  (2, 0),
+>  (3, NULL),
+>  (4, 5);
--- 3 row(s) inserted.
>>CREATE TABLE LITEREG022_SPACE_RESULTS
+>  (id INT PRIMARY KEY,
+>   space_txt VARCHAR(8),
+>   prefixed_txt VARCHAR(9),
+>   wrapped_txt VARCHAR(10));
--- SQL operation complete.
>>INSERT INTO LITEREG022_SPACE_RESULTS VALUES
+>  (1, SPACE(3), 'Z' || SPACE(3), 'L' || SPACE(3) || 'R');
--- 1 row(s) inserted.
>>INSERT INTO LITEREG022_SPACE_RESULTS
+>SELECT id,
+>       SPACE(space_count),
+>       'Z' || SPACE(space_count),
+>       'L' || SPACE(space_count) || 'R'
+>  FROM LITEREG022_SPACE_COUNTS;
--- 3 row(s) inserted.
>>SELECT id,
+>       CHAR_LENGTH(space_txt) AS space_count,
+>       CHAR_LENGTH(prefixed_txt) AS prefixed_count,
+>       CHAR_LENGTH(wrapped_txt) AS wrapped_count,
+>       CASE WHEN space_txt IS NULL THEN 'NULL' ELSE 'VALUE' END AS value_state
+>  FROM LITEREG022_SPACE_RESULTS
+> ORDER BY id;
ID           SPACE_COUNT  PREFIXED_COUNT  WRAPPED_COUNT  VALUE_STATE
-----------  -----------  --------------  -------------  -----------
          1            3               4              5  VALUE
          2            0               1              2  VALUE
          3            ?               ?              ?  NULL
          4            5               6              7  VALUE
--- 4 row(s) selected.
>>SELECT id
+>  FROM LITEREG022_SPACE_RESULTS
+> WHERE space_txt = SPACE(3)
+> ORDER BY id;
ID
-----------
          1
          2
          4
--- 3 row(s) selected.
>>SELECT id
+>  FROM LITEREG022_SPACE_RESULTS
+> WHERE CHAR_LENGTH(space_txt) = 3
+> ORDER BY id;
ID
-----------
          1
--- 1 row(s) selected.
>>SELECT id
+>  FROM LITEREG022_SPACE_RESULTS
+> WHERE prefixed_txt = 'Z' || SPACE(3)
+> ORDER BY id;
ID
-----------
          1
          2
          4
--- 3 row(s) selected.
>>SELECT id
+>  FROM LITEREG022_SPACE_RESULTS
+> WHERE wrapped_txt = 'L' || SPACE(3) || 'R'
+> ORDER BY id;
ID
-----------
          1
--- 1 row(s) selected.
>>SELECT CHAR_LENGTH(SPACE(2147483647)) AS oversized_count
+>  FROM (VALUES (1)) AS v(n);
*** ERROR[4129]
*** ERROR[16001] No message found for SQLCODE -4129. REPEAT <MXID>
*** ERROR[4062]
*** ERROR[16001] No message found for SQLCODE -4062. SPACE <MXID>
*** ERROR[8822]
*** ERROR[16001] No message found for SQLCODE -8822. <MXID>
>>SELECT id, CHAR_LENGTH(wrapped_txt) AS recovered_count
+>  FROM LITEREG022_SPACE_RESULTS
+> WHERE id = 4;
ID           RECOVERED_COUNT
-----------  ---------------
          4                7
--- 1 row(s) selected.
>>DROP TABLE LITEREG022_SPACE_RESULTS;
--- SQL operation complete.
>>DROP TABLE LITEREG022_SPACE_COUNTS;
--- SQL operation complete.
>>exit;
