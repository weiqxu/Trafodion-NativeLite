CREATE TABLE TPCC_WAREHOUSE (
  W_ID INT NOT NULL PRIMARY KEY,
  W_NAME VARCHAR(10) NOT NULL,
  W_STREET_1 VARCHAR(20) NOT NULL,
  W_STREET_2 VARCHAR(20) NOT NULL,
  W_CITY VARCHAR(20) NOT NULL,
  W_STATE CHAR(2) NOT NULL,
  W_ZIP CHAR(9) NOT NULL,
  W_TAX DECIMAL(4,4) NOT NULL,
  W_YTD DECIMAL(12,2) NOT NULL
);

CREATE TABLE TPCC_DISTRICT (
  D_W_ID INT NOT NULL,
  D_ID INT NOT NULL,
  D_NAME VARCHAR(10) NOT NULL,
  D_STREET_1 VARCHAR(20) NOT NULL,
  D_STREET_2 VARCHAR(20) NOT NULL,
  D_CITY VARCHAR(20) NOT NULL,
  D_STATE CHAR(2) NOT NULL,
  D_ZIP CHAR(9) NOT NULL,
  D_TAX DECIMAL(4,4) NOT NULL,
  D_YTD DECIMAL(12,2) NOT NULL,
  D_NEXT_O_ID INT NOT NULL,
  PRIMARY KEY (D_W_ID, D_ID),
  CONSTRAINT TPCC_DISTRICT_W_FK FOREIGN KEY (D_W_ID)
    REFERENCES TPCC_WAREHOUSE (W_ID)
);

CREATE TABLE TPCC_ITEM (
  I_ID INT NOT NULL PRIMARY KEY,
  I_IM_ID INT NOT NULL,
  I_NAME VARCHAR(24) NOT NULL,
  I_PRICE DECIMAL(5,2) NOT NULL,
  I_DATA VARCHAR(50) NOT NULL
);

CREATE TABLE TPCC_CUSTOMER (
  C_W_ID INT NOT NULL,
  C_D_ID INT NOT NULL,
  C_ID INT NOT NULL,
  C_FIRST VARCHAR(16) NOT NULL,
  C_MIDDLE CHAR(2) NOT NULL,
  C_LAST VARCHAR(16) NOT NULL,
  C_STREET_1 VARCHAR(20) NOT NULL,
  C_STREET_2 VARCHAR(20) NOT NULL,
  C_CITY VARCHAR(20) NOT NULL,
  C_STATE CHAR(2) NOT NULL,
  C_ZIP CHAR(9) NOT NULL,
  C_PHONE CHAR(16) NOT NULL,
  C_SINCE TIMESTAMP NOT NULL,
  C_CREDIT CHAR(2) NOT NULL,
  C_CREDIT_LIM DECIMAL(12,2) NOT NULL,
  C_DISCOUNT DECIMAL(4,4) NOT NULL,
  C_BALANCE DECIMAL(12,2) NOT NULL,
  C_YTD_PAYMENT DECIMAL(12,2) NOT NULL,
  C_PAYMENT_CNT INT NOT NULL,
  C_DELIVERY_CNT INT NOT NULL,
  C_DATA VARCHAR(500) NOT NULL,
  PRIMARY KEY (C_W_ID, C_D_ID, C_ID),
  CONSTRAINT TPCC_CUSTOMER_D_FK FOREIGN KEY (C_W_ID, C_D_ID)
    REFERENCES TPCC_DISTRICT (D_W_ID, D_ID)
);

CREATE INDEX TPCC_CUSTOMER_NAME_IX
  ON TPCC_CUSTOMER (C_W_ID, C_D_ID, C_LAST, C_FIRST);

CREATE TABLE TPCC_HISTORY (
  H_ID NUMERIC(18,0) NOT NULL PRIMARY KEY,
  H_C_ID INT NOT NULL,
  H_C_D_ID INT NOT NULL,
  H_C_W_ID INT NOT NULL,
  H_D_ID INT NOT NULL,
  H_W_ID INT NOT NULL,
  H_DATE TIMESTAMP NOT NULL,
  H_AMOUNT DECIMAL(6,2) NOT NULL,
  H_DATA VARCHAR(24) NOT NULL,
  CONSTRAINT TPCC_HISTORY_C_FK FOREIGN KEY (H_C_W_ID, H_C_D_ID, H_C_ID)
    REFERENCES TPCC_CUSTOMER (C_W_ID, C_D_ID, C_ID)
);

CREATE INDEX TPCC_HISTORY_CUSTOMER_IX
  ON TPCC_HISTORY (H_C_W_ID, H_C_D_ID, H_C_ID);

CREATE TABLE TPCC_ORDERS (
  O_W_ID INT NOT NULL,
  O_D_ID INT NOT NULL,
  O_ID INT NOT NULL,
  O_C_ID INT NOT NULL,
  O_ENTRY_D TIMESTAMP NOT NULL,
  O_CARRIER_ID INT,
  O_OL_CNT INT NOT NULL,
  O_ALL_LOCAL INT NOT NULL,
  PRIMARY KEY (O_W_ID, O_D_ID, O_ID),
  CONSTRAINT TPCC_ORDERS_C_FK FOREIGN KEY (O_W_ID, O_D_ID, O_C_ID)
    REFERENCES TPCC_CUSTOMER (C_W_ID, C_D_ID, C_ID)
);

CREATE INDEX TPCC_ORDERS_CUSTOMER_IX
  ON TPCC_ORDERS (O_W_ID, O_D_ID, O_C_ID, O_ID);

CREATE TABLE TPCC_NEW_ORDER (
  NO_W_ID INT NOT NULL,
  NO_D_ID INT NOT NULL,
  NO_O_ID INT NOT NULL,
  PRIMARY KEY (NO_W_ID, NO_D_ID, NO_O_ID),
  CONSTRAINT TPCC_NEW_ORDER_O_FK FOREIGN KEY (NO_W_ID, NO_D_ID, NO_O_ID)
    REFERENCES TPCC_ORDERS (O_W_ID, O_D_ID, O_ID)
);

CREATE TABLE TPCC_STOCK (
  S_W_ID INT NOT NULL,
  S_I_ID INT NOT NULL,
  S_QUANTITY INT NOT NULL,
  S_DIST_01 CHAR(24) NOT NULL,
  S_DIST_02 CHAR(24) NOT NULL,
  S_DIST_03 CHAR(24) NOT NULL,
  S_DIST_04 CHAR(24) NOT NULL,
  S_DIST_05 CHAR(24) NOT NULL,
  S_DIST_06 CHAR(24) NOT NULL,
  S_DIST_07 CHAR(24) NOT NULL,
  S_DIST_08 CHAR(24) NOT NULL,
  S_DIST_09 CHAR(24) NOT NULL,
  S_DIST_10 CHAR(24) NOT NULL,
  S_YTD INT NOT NULL,
  S_ORDER_CNT INT NOT NULL,
  S_REMOTE_CNT INT NOT NULL,
  S_DATA VARCHAR(50) NOT NULL,
  PRIMARY KEY (S_W_ID, S_I_ID)
);

CREATE TABLE TPCC_ORDER_LINE (
  OL_W_ID INT NOT NULL,
  OL_D_ID INT NOT NULL,
  OL_O_ID INT NOT NULL,
  OL_NUMBER INT NOT NULL,
  OL_I_ID INT NOT NULL,
  OL_SUPPLY_W_ID INT NOT NULL,
  OL_DELIVERY_D TIMESTAMP,
  OL_QUANTITY INT NOT NULL,
  OL_AMOUNT DECIMAL(6,2) NOT NULL,
  OL_DIST_INFO CHAR(24) NOT NULL,
  PRIMARY KEY (OL_W_ID, OL_D_ID, OL_O_ID, OL_NUMBER)
);
